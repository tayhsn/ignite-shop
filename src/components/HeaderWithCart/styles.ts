import { styled } from '../../styles';

export const HeaderContainer = styled('header', {
   width: '100%',
   maxWidth: 1180,

   margin: '0 auto',
   padding: '2rem',

   display: 'flex',
   justifyContent: 'space-between',
});
